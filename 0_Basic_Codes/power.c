#include<stdio.h>
#include<math.h>
int main()
{
	int a, b;
	
	printf("the value of a is\n ");
	scanf("%d",&a);
	
	printf(" the value of b is\n ");
	scanf("%d",&b);
	
	printf(" the power of a to the 2 is %f\n",pow(a,2));
	printf(" the power of a to the 3 is %f",pow(b,3));
	
	return 0;
}
