#include<stdio.h>
#include<math.h>
int main()
{
    int side;
    printf("enter the valu of side of a square \n");
    scanf("%d",&side);
    printf("the area of square is %f",pow(side,2)); // here %f is used if we use it will
                                                   // return 0 value

return 0;

}